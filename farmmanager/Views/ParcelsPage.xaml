<Page
    x:Class="farmmanager.Views.ParcelsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="32,24" ColumnSpacing="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="340"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Form -->
        <Border Grid.Column="0" Style="{StaticResource CardStyle}" VerticalAlignment="Top">
            <StackPanel Spacing="14">
                <Grid>
                    <TextBlock Text="Plantation Parcels" Style="{StaticResource PageTitleStyle}"/>
                    <Button Content="+ New" HorizontalAlignment="Right" VerticalAlignment="Center" Click="New_Click"/>
                </Grid>

                <TextBlock Text="PARCEL NAME *" Style="{StaticResource SectionHeaderStyle}"/>
                <TextBox x:Name="NameTxt" PlaceholderText="e.g. North Block A"/>

                <TextBlock Text="CODE" Style="{StaticResource SectionHeaderStyle}"/>
                <TextBox x:Name="CodeTxt" PlaceholderText="e.g. NBA-01"/>

                <TextBlock Text="AREA (HECTARES)" Style="{StaticResource SectionHeaderStyle}"/>
                <NumberBox x:Name="AreaBox" Minimum="0" PlaceholderText="0.00" SpinButtonPlacementMode="Compact"/>

                <TextBlock Text="DESCRIPTION" Style="{StaticResource SectionHeaderStyle}"/>
                <TextBox x:Name="DescTxt" PlaceholderText="Optional description..."
                         TextWrapping="Wrap" Height="64" AcceptsReturn="True"/>

                <Grid ColumnSpacing="8" Margin="0,4,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Save Parcel" HorizontalAlignment="Stretch"
                            Style="{StaticResource AccentButtonStyle}" Click="Save_Click"/>
                    <Button Grid.Column="1" Content="Deactivate"
                            x:Name="DeactivateBtn" Visibility="Collapsed" Click="Deactivate_Click"/>
                </Grid>
                <TextBlock x:Name="StatusTxt" FontSize="12"
                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
            </StackPanel>
        </Border>

        <!-- List -->
        <Border Grid.Column="1" Style="{StaticResource CardStyle}">
            <StackPanel Spacing="12">
                <TextBlock Text="All Parcels" FontSize="15" FontWeight="SemiBold"/>
                <ListView x:Name="ParcelsList" SelectionChanged="Parcels_SelectionChanged">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="12,8"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <Border Grid.Column="1"
                                        Background="{ThemeResource AccentFillColorTertiaryBrush}"
                                        CornerRadius="4" Padding="8,2"
                                        HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Code}" FontSize="12"/>
                                </Border>
                                <TextBlock Grid.Column="2"
                                    Text="{Binding AreaHectares}"
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>
    </Grid>
</Page>
